<!DOCTYPE html>
<html>
  <head>
    <title>选择老师</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/lib/layui/css/layui.css">
    <link rel="stylesheet" href="/stylesheets/selectTeacher.css">
    <script src="/lib/jquery-3.1.1.min.js"></script>
    <script src="/lib/layui/layui.js"></script>
  </head>
  <body>
    <div class="container">
      <div class="layui-form">
        <select name="teacher" lay-filter="test" id="selectTeacher">
            <option value="">请选择一个老师</option>
            <% for (var i in teachers) { %>
            <option value="<%= teachers[i].tno %>"><%= teachers[i].tname %></option>
            <% } %>
        </select>
      </div>
    </div>
    <script src="/javascripts/selectTeacher.js"></script>
  </body>
</html>
